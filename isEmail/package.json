{
  "name": "@vanguard/is-email",
  "version": "1.0.0",
  "description": "A fast email checker.",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "rm -rf dist && rollup -c ../rollup.config.js --environment production",
    "test": "ava",
    "test:watch": "ava --watch"
  },
  "ava": {
    "require": [
      "esm"
    ]
  },
  "devDependencies": {}
}
